---
milestone: v1.2
audited: 2026-04-20T00:00:00Z
status: tech_debt
scores:
  requirements: 18/18
  phases: 6/6
  integration: 8/8
  flows: 8/8
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "13"
    items:
      - "This audit supersedes the earlier v1.2 audit conclusions after Phase 13 closure. Keep only the latest report as the source of truth going forward."
  - phase: "12"
    items:
      - "Historical Phase 12 verification wording still says image watermark was not exposed at that verification point; current shipped behavior is now default-off and manually enableable."
  - phase: "12-13"
    items:
      - "Internal compatibility fields remain in image reference handling (base64/data URL and fixedCollection-style fallback) even though the public UI now intentionally exposes only URL and binary."
nyquist:
  overall: skipped
  reason: "workflow.nyquist_validation is explicitly false in GSD config."
---

# Milestone v1.2 Audit

## Result

**Status:** tech_debt

v1.2 now satisfies its milestone definition of done from a functional, verification, and cross-phase integration perspective.

No current blocker remains.

The previous blocker classes from the earlier audit have been closed by Phase 13:

- Phase 08-11 now each have a formal `*-VERIFICATION.md`
- `IMG-04` has been reconciled to the shipped public contract: public UI supports URL/binary, while base64/data URL remains internal compatibility fallback
- `VAL-IMG-02` is now checked and formally traced
- image watermark scope is aligned to shipped default-off behavior
- the final execute-level `max_images` request-shaping regression is locked

What remains is non-blocking technical/planning debt only.

## Scope

Audited milestone: v1.2 Seedream 5.0 lite image generation, UX iteration, and audit reconciliation.

Phases in scope:

| Phase | Directory | Summary | Verification |
| --- | --- | --- | --- |
| 08 | `.planning/phases/08-seedream-image-operation-contract-ux-skeleton` | present | passed |
| 09 | `.planning/phases/09-image-payload-builder-input-normalization-validation` | present | passed |
| 10 | `.planning/phases/10-image-execution-path-binary-output-partial-failure-mapping` | present | passed |
| 11 | `.planning/phases/11-regression-coverage-documentation-release-hardening` | present | passed |
| 12 | `.planning/phases/12-image-generation-mode-operation-ux-refactor` | present | passed |
| 13 | `.planning/phases/13-v1-2-audit-reconciliation-verification-closure` | present | passed |

## Requirement Coverage

| Requirement | Final Status | Evidence |
| --- | --- | --- |
| IMG-01 | satisfied | Mode-first image entry exists and is verified in Phase 12. |
| IMG-02 | satisfied | Fixed Seedream 5.0 Lite model is wired and verified. |
| IMG-03 | satisfied | Text-to-image flow is wired, tested, and verified. |
| IMG-04 | satisfied | Public UI contract now explicitly matches shipped URL/binary behavior; base64 remains internal fallback only. |
| IMG-05 | satisfied | Multi-reference URL/binary input and count constraints are wired and tested. |
| IMG-06 | satisfied | Group-image toggle and max image constraints are wired and now execute-level locked. |
| IMG-07 | satisfied | Web search, prompt optimization, and size mapping are wired. |
| IMG-08 | satisfied | Default binary image output is wired through result mapper. |
| IMG-09 | satisfied | JSON metadata and per-image failures are preserved. |
| IMG-10 | satisfied | Video non-regression remains verified. |
| UX-IMG-01 | satisfied | Image mode UI grouping and ordering are verified. |
| UX-IMG-02 | satisfied | Common image path remains primary and progressive disclosure remains intact. |
| UX-IMG-03 | satisfied | 24-hour asset warning remains documented and tested. |
| UX-IMG-04 | satisfied | Streaming remains unexposed. |
| VAL-IMG-01 | satisfied | Runtime validation covers reference count, MIME, size, and sources. |
| VAL-IMG-02 | satisfied | Recommended resolution/aspect-ratio combinations are validated and now formally traced. |
| VAL-IMG-03 | satisfied | Group-mode `max_images` and total image constraints are wired and tested. |
| VAL-IMG-04 | satisfied | Partial failures are preserved in output JSON. |

Score: 18/18 requirements satisfied.

## Cross-Phase Integration

Integration checker status: `tech_debt`.

Connected flows:

| Flow | Status | Evidence |
| --- | --- | --- |
| Text-to-image | passed | UI → execute → payload → mapper → binary output |
| Image-to-image URL | passed | Public URL source selector, comma splitting, payload normalization |
| Image-to-image binary | passed | Public binary source selector, binary normalization, payload mapping |
| Public image-to-image UI scope | passed | Public contract intentionally limited to URL / binary |
| Group image generation | passed | UI toggle, validator, payload options, execute-level max_images assertion |
| Partial image failure | passed | Per-image failures preserved in `json.images[]` |
| All-failed image response | passed | Aggregated clear error behavior |
| Video non-regression | passed | Existing video create/get/list/delete flow remains intact |

## Remaining Tech Debt

### 1. Historical Audit Supersession

This report supersedes the previous `gaps_found` version of the v1.2 audit after Phase 13 closure.

Recommendation:

- Treat this file as the canonical audit result going forward.

### 2. Historical Phase 12 Verification Wording Drift

Phase 12's historical verification still reflects an earlier point-in-time statement that image watermark was not exposed.

Current shipped behavior is now:

- image watermark default-off
- manually enableable when needed

This is not a product gap, only historical report drift.

### 3. Internal Compatibility Residue

The code still keeps some internal compatibility/fallback handling for image reference sources such as base64/data URL and fixedCollection-style flows, even though the public UI intentionally exposes only URL and binary.

This is acceptable for backward compatibility, but can be cleaned later if desired.

## Nyquist Compliance

Skipped because `workflow.nyquist_validation` is explicitly false in the current GSD config.

## Final Recommendation

v1.2 no longer has blocking milestone gaps.

It is ready for milestone completion, with only non-blocking debt noted above.
